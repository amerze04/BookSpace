// Local dev only for now — no fileReplacements/production split yet, since
// there's no deployed frontend target this WP. Add one when that changes.
export const environment = {
  apiBaseUrl: 'http://localhost:5270',
};
